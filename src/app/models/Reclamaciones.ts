import { Recompensas } from "./Recompensas";
import { Usuario } from "./Usuario";

export class Reclamaciones {
    idReclamacion: number=0;
    recompensa: Recompensas=new Recompensas();
    usuario: Usuario=new Usuario()
}